from setuptools import setup

setup(
    name='RandomPyCharGen',
    version='0.0.1',
    packages=['virtual', 'virtual.RandomCharGen'],
    url='https://github.com/borkusgod',
    license='LPGL',
    author='William Bourque',
    author_email='',
    description='A simple python program that creates a string of random '
                'characters and places them into a data collection of your '
                'choice. '
)
